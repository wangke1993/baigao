export class <%=getBigModuleTitle(moduleConf.nameEn) %>Dto {

    _id?: string;
    /**
     * 最后更新用户
     */
    addUser?: string;
    
    /**
     * 添加时间
     */
    addDate?: string;
    
    /**
     * 最后更新用户
     */
    updateUser?: string;
    
    /**
     * 最后更新时间
     */
    updateDate?: string;
    
    /**
     * 文件id数组，只要文章上传文件，保留原有数据且把上传的文件id都push到该数组中。
     */
    fileIds?: string[];
    
    <% if(conf.config.UUID){%>
    /**
    * UUID
    */
    UUID!: string;
    <% } %>
    <% for (let i = 0; i < fieldList.length; i++) { %>
    /**
     * <%=fieldList[i].name %>
     */
     <%=fieldList[i].nameEn %><%=fieldList[i].notNull?"!":"?" %>: <%=fieldList[i].type %>;
    <% } %>
}